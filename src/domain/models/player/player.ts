export type Player = {
    readonly _id: string
    readonly name: string
    readonly email: string
    phoneNumber: string
    ranking: string
    rankingPosition: number
    urlPhotoPlayer: string
}